interface IState {
	isGarageOpen: Boolean,
	lightColor: String,
	isLightOn: Boolean,
	manual: Boolean,
	isLocked: Boolean,
	LivingRoomWindow: Boolean,
	isRaining: Boolean,
	isSunny: Boolean,
	temp: Number,

}

export default IState;