import { Radio } from 'react-loader-spinner';
function Spinner(): JSX.Element{
    return (
        <div className="spinner">
            <Radio height={50} width={50} />
        </div>
    );
}

export default Spinner;