import Button from './Button';

function Controls(): JSX.Element {
    return (
        <>
            <Button onClick={() => {}}>
                Turn On Light
            </Button>
        </>
    );
}

export default Controls;
